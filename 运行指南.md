# HandNote Android 项目运行指南

## 📱 需要的软件

### 1. Android Studio（必需）
- **下载地址**: https://developer.android.com/studio
- **版本要求**: Android Studio Hedgehog (2023.1.1) 或更高版本
- **包含内容**:
  - Android SDK
  - Android 模拟器
  - Gradle 构建工具
  - Kotlin 编译器

### 2. Java Development Kit (JDK)（必需）
- **版本要求**: JDK 17 或更高版本
- **说明**: Android Studio 通常自带 JDK，但也可以单独安装
- **下载地址**: https://www.oracle.com/java/technologies/downloads/ 或使用 OpenJDK

## 🚀 快速开始

### 方法一：使用 Android Studio（推荐）

1. **打开项目**
   - 启动 Android Studio
   - 选择 `File` → `Open`
   - 选择项目目录：`/Users/leocham/Documents/code/Agent_dev/HandNote`
   - 点击 `OK`

2. **等待同步**
   - Android Studio 会自动检测项目并开始同步
   - 首次打开会下载 Gradle 和依赖项（可能需要几分钟）
   - 等待底部状态栏显示 "Gradle build finished"

3. **配置 Android SDK**（如果需要）
   - 如果提示缺少 SDK，选择 `Tools` → `SDK Manager`
   - 确保安装了：
     - Android SDK Platform 34
     - Android SDK Build-Tools
     - Android Emulator

4. **运行应用**
   - 点击工具栏的绿色运行按钮 ▶️
   - 或按快捷键 `Shift + F10` (Windows/Linux) 或 `Ctrl + R` (Mac)
   - 选择设备：
     - **模拟器**: 如果没有设备，Android Studio 会提示创建虚拟设备
     - **真机**: 连接 Android 手机，开启 USB 调试模式

### 方法二：使用命令行

#### macOS/Linux:

```bash
# 1. 进入项目目录
cd /Users/leocham/Documents/code/Agent_dev/HandNote

# 2. 确保 gradlew 有执行权限（已自动设置）
chmod +x gradlew

# 3. 构建项目
./gradlew build

# 4. 安装到连接的设备（需要先连接手机或启动模拟器）
./gradlew installDebug

# 5. 或者直接运行（需要先启动模拟器或连接设备）
./gradlew installDebug && adb shell am start -n com.handnote.app/.MainActivity
```

#### Windows:

```cmd
# 1. 进入项目目录
cd C:\path\to\HandNote

# 2. 构建项目
gradlew.bat build

# 3. 安装到连接的设备
gradlew.bat installDebug
```

## 📋 创建 Android 模拟器

如果没有真机，需要创建虚拟设备：

1. 在 Android Studio 中，点击 `Tools` → `Device Manager`
2. 点击 `Create Device`
3. 选择设备型号（推荐：Pixel 5 或 Pixel 6）
4. 选择系统镜像（推荐：API 34，Android 14）
5. 点击 `Finish`
6. 启动模拟器后，点击运行按钮即可安装应用

## 🔧 常见问题

### 1. Gradle 同步失败
- **问题**: 网络问题导致依赖下载失败
- **解决**: 
  - 检查网络连接
  - 尝试使用 VPN 或代理
  - 在 `gradle.properties` 中添加代理配置

### 2. SDK 未找到
- **问题**: 提示找不到 Android SDK
- **解决**: 
  - 打开 `File` → `Project Structure` → `SDK Location`
  - 设置 Android SDK 路径（通常在 `~/Library/Android/sdk` on Mac）

### 3. 编译错误：找不到符号
- **问题**: Room 或其他库的代码生成失败
- **解决**: 
  - 清理项目：`Build` → `Clean Project`
  - 重新构建：`Build` → `Rebuild Project`

### 4. 模拟器启动慢
- **问题**: 首次启动模拟器很慢
- **解决**: 
  - 这是正常现象，首次启动需要初始化
  - 后续启动会快很多
  - 建议保持模拟器运行，不要频繁关闭

## 📱 应用功能

应用启动后会显示底部导航栏，包含 4 个 Tab：
- **日程 (Calendar)**: 显示日程相关功能
- **沉淀 (Feed)**: 显示帖子/记录
- **配置 (Config)**: 应用配置
- **设置 (Settings)**: 应用设置

## 🛠️ 开发工具推荐

- **Android Studio**: 官方 IDE，功能最全
- **VS Code**: 轻量级编辑器，需要安装 Kotlin 和 Android 扩展
- **IntelliJ IDEA**: JetBrains 的 IDE，与 Android Studio 同源

## 📚 相关资源

- [Android 官方文档](https://developer.android.com/)
- [Jetpack Compose 文档](https://developer.android.com/jetpack/compose)
- [Room 数据库文档](https://developer.android.com/training/data-storage/room)
- [Kotlin 官方文档](https://kotlinlang.org/docs/home.html)

## ✅ 验证安装

运行以下命令验证环境是否正确配置：

```bash
# 检查 Java 版本（应该是 17 或更高）
java -version

# 检查 Android SDK（如果已安装）
echo $ANDROID_HOME  # macOS/Linux
echo %ANDROID_HOME%  # Windows

# 检查 Gradle（应该显示 8.2）
./gradlew --version
```

---

**提示**: 首次运行可能需要 5-10 分钟来下载所有依赖项，请耐心等待。

